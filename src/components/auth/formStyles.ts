// Class strings shared by more than one form, so the inputs, the submit buttons
// and the error text stay identical across them. A style used by a single
// component stays in that component.

export const inputClasses =
  'w-full rounded border border-gray-300 px-3 py-2 outline-none focus:border-gray-900';

export const submitButtonClasses =
  'rounded bg-gray-900 px-4 py-2 font-medium text-white disabled:opacity-60';

export const errorTextClasses = 'text-sm text-red-600';
